# Frontend (Vite) — copy to `.env`
# Restart Vite after changing VITE_* values.

# Always use relative /api (Vite proxy locally, Vercel serverless in production).
# Do NOT set an absolute external URL — the frontend always uses /api.
VITE_API_URL=/api
