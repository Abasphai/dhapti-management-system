# Use Vite proxy in development (recommended).
# Restart Vite after changing this file.
VITE_API_URL=/api

# Emergency override if the Vite proxy is unavailable:
# VITE_API_URL=http://127.0.0.1:4000/api
